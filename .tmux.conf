# unbinds
unbind C-b
unbind b

# set C-a as prefix key
set-option -g prefix C-a

# pass through C-a
bind-key a send-prefix

# enable ctrl-arrow keys
set-window-option -g xterm-keys on

# set the status bar
set-option -g status-style bg=default,fg=white
set-option -g status-left ''
set-option -g status-right ''
set-window-option -g window-status-current-style bg=red

# toggle between windows
bind-key C-a last-window

# set notifications
setw -g monitor-activity on
set -g visual-activity on

# automatically set the title
#setw -g automatic-rename on
#set -g automatic-rename-format "#{pane_current_command}"

# split panes
bind-key | split-window -h -c "#{pane_current_path}"
bind-key - split-window -v -c "#{pane_current_path}"

# faster key repitition
set-option -s escape-time 0

# all shortcuts will use the vi mode shortcuts
set-window-option -g mode-keys vi

# toggle between two panes
bind-key l select-pane -R
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U

# set tmux colorscheme for solarized
set -g default-terminal "xterm-256color"

# open new windows in the same directory
bind-key c new-window -c "#{pane_current_path}"

# set max scroll histor limit
set-option -g history-limit 10000

# even splitting (default)
#bind-key M-1 select-layout even-vertical
#bind-key M-2 select-layout even-horizontal

# Setup 'v' to begin selection as in Vim
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection-and-cancel

# Update default binding of `Enter` to also use copy-pipe
#unbind -t vi-copy Enter
#bind-key -t vi-copy Enter copy-pipe "reattach-to-user-namespace pbcopy"

# ensure mouse copy respects panes
# this messes up clicking for links!
#set -g mouse on

run ~/.config/tmux/plugins/catppuccin/tmux/catppuccin.tmux

set -g @catppuccin_flavor "macchiato"

## Active window: real-time process
set -g @catppuccin_window_current_text '#W'
set -g @catppuccin_window_default_text '#W'
#set -g @catppuccin_pane_default_text "T"
#set -g @catppuccin_window_default_text "W"
#
## Non-current windows: prefer tmux's window_name (set by automatic-rename),
## which will be the last useful command name; otherwise show pane_current_command.
#set -g @catppuccin_window_text "#{?#{==:#{window_name},},#{pane_current_command},#{window_name}}"
#
## Keep automatic renaming so window_name gets meaningful values
#set -g automatic-rename on
#set -g automatic-rename-format "#{pane_current_command}"
#
##set -g window-status-current-format "#{pane_current_command}"
##set -g window-status-format "#{pane_current_command}"
#
##set -g @catppuccin_window_default_text '#W'
##set -g @catppuccin_window_current_text '#W'
#
##set -g @catppuccin_window_default_text " #{window_name}"
##set -g @catppuccin_window_status "icon"
###set -g @catppuccin_window_default_fill "number"
#set -g @catppuccin_window_number_position "left"

## Window current configuration
#set -g @catppuccin_window_current_text "#{window_name}"
#set -g @catppuccin_window_current_fill "all"
#set -g @catppuccin_window_current_middle_separator "#[reverse] 󰿟 #[noreverse]"

#set -g status-right-length 100
#set -g status-left-length 100
#set -g status-left ""
#set -g status-right "#{E:@catppuccin_status_application}"
